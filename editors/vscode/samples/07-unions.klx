// union = "e' esattamente una di queste forme", controllata a esaustivita' da match.
union Shape { Circle(Int), Rectangle(Int, Int) };

let area = func(Shape s) =>
    match s with Circle(r) => r * r * 3 | Rectangle(w, h) => w * h;

area (Rectangle 3 5)
